<dt>Review</dt>
<dd><%= review.description %></dd>
<dt></dt>
<dt>Rating</dt>
<dd><%= review.rating %></dd>
<dt></dt>
<dd>

<% if current_user.present? && current_user.id == review.user_id %>
<%= link_to "Delete Review", "./#{:product_id}/reviews/#{review.id}", controller: "review", method: :delete, data: { confirm: 'Are you sure?' }%>
<% end %>

</dd>